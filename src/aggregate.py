import src.main as main

# How many businesses are there for headingcode ASK result in numberOfBusinesses
def business_heading_ask() -> list[str]:
    return main.aggregate([''])

# What business has the most projects as _id, numberOfProjects
def business_most_projects() -> list[str]:
    return main.aggregate([''])

# Merge the phoneList and mobileList array’s for businessId 1 into phones
def merge_phone_lists() -> list[str]:
    return main.aggregate([''])

# Count the number of projects for each programType as _id, numberOfProjects
def count_projects_program_type() -> list[str]:
    return main.aggregate([''])